Tried to create a very simple version of Dino game using only HTML, CSS, JS. 
There is still a bug: If user presses spacebar more than once, then score is incremneted that many times. 
There needs to be a logic that increments score not for just jumping, but for jumping over the block.
